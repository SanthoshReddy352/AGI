# --- CORE GUI & UTILS ---
PyQt6
PyYAML==6.0.1
psutil
PyAutoGUI
Pillow
huggingface_hub
python-dotenv
requests

# --- VOICE I/O (LOCAL PIPELINE) ---
faster-whisper
llama-cpp-python
numpy
sounddevice
pyttsx3
# Wake word ("Hey Friday") — Porcupine engine + its bundled recorder.
# Needs a free Picovoice access key in FRIDAY_PORCUPINE_KEY (see .env.example).
# Cross-platform wheels ship for Linux / Windows / macOS / Raspberry Pi.
pvporcupine
pvrecorder

# --- RESEARCH AGENT ---
trafilatura
feedparser
html2text
beautifulsoup4
lxml
selectolax
httpx[http2]

# --- AGENTIC FRAMEWORK & STORAGE ---
langgraph
langchain-core
chromadb
sentence-transformers

# --- DOCUMENT INTELLIGENCE ---
markitdown[pdf]

# --- INTENT & ENTITY ---
rapidfuzz
dateparser

# --- BROWSER AUTOMATION ---
playwright
selenium
webdriver-manager

# --- CLI INTERFACE ---
prompt-toolkit
